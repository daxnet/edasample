import { environment } from 'src/environments/environment';

export class Product {
    description: string;
    id: string;
    name: string;
    pictureFileName: string;
    price: number;
    unit: string;
    inventory: number;
}
